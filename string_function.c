#include<stdio.h>
#include<string.h>
void main()
{
    char s1[] = "Govind";
    char s2[] = "ajjwa";
    char s3[34];
    // puts(strcat(s1 ,s2));
    // printf("The length of s1 is %d\n",strlen(s1));
    // printf("The length of s1 is %d\n",strlen(s2));
    // puts(strrev(s1));
    // strcpy(s3,strcat(s1,s2));
    // puts(s3);
    printf("The Strcmp for s1 and s2 %d",strcmp(s1,s2));



}